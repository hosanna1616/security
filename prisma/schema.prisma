generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Message {
  id        Int      @id @default(autoincrement())
  email     String
  content   String
  seen      Boolean  @default(false)
  createdAt DateTime @default(now())
}

model Service {
  id        Int      @id @default(autoincrement())
  name      String
  status    String   // "running" | "degraded" | "down"
  updatedAt DateTime @updatedAt
}

